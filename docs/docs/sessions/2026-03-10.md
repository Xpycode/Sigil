# Session: 2026-03-10

## Goal
Install codebase-memory-mcp + set up persistent Apple developer credentials.

## Progress
- Evaluated codebase-memory-mcp vs Serena (complementary tools, not competing)
- Downloaded and installed binary v0.4.6 to `~/.local/bin/`
- Ran `codebase-memory-mcp install` — auto-registered with all detected editors:
  - Claude Code, Codex CLI, Cursor, Windsurf, Gemini CLI, VS Code, Zed
- 4 Claude skills installed: exploring, tracing, quality, reference
- Database location: `~/.cache/codebase-memory-mcp/`
- Auto-sync (filesystem watcher) is built-in after first index
- Created `~/.claude/apple-developer.md` with persistent Apple developer credentials
- Searched all Xcode projects to auto-populate: Team ID, bundle prefixes, signing identities
- Added reference in global `~/.claude/CLAUDE.md` so Claude always reads credentials
- Corrected Team Name to GREGOR MÜLLER, primary bundle prefix to `com.lucesumbrarum`

## Decisions
- Keep Serena alongside codebase-memory-mcp (Serena for editing, CMM for exploration/tracing)
- Installed CMM globally (user scope) so it's available across all projects
- Apple credentials stored at `~/.claude/apple-developer.md` (local-only, never committed)
- Bundle ID prefix: always check project's actual ID, don't default to `com.xpycode`

## Next
- Restart Claude Code to activate codebase-memory-mcp MCP server
- Index Swift projects to build knowledge graphs
- Add App Store Connect API key when available
