import XCTest
@testable import Sigil

final class SigilTests: XCTestCase {

    /// Wave 0 placeholder — proves the test target compiles and runs.
    /// Replaced in Wave 2 with VolumeStoreTests.
    func testSkeleton() {
        XCTAssertEqual(1 + 1, 2)
    }
}
